# Python Calculator (CLI)

A simple **command-line calculator** written in **Python**.  
It supports basic arithmetic operations along with power and modulo calculations, with input validation and error handling.

## 🚀 Features
- Addition (`+`)
- Subtraction (`-`)
- Multiplication (`*`)
- Division (`/`)
- Power (`**`)
- Modulo (`%`)
- Division by zero protection
- Invalid operation handling
- Continuous calculation loop until exit

## 🛠 Technologies Used
- Python 3
- Standard input/output (CLI)

## ⚙️ Requirements
- **Python 3.7** or higher

## ▶️ Run the Program
```bash
python calculator.py
